const API_HOST = '3.129.251.86';
const API_PORT = 3000;
const API_VERSION = 'v1';
const API_URL = `http://${API_HOST}:${API_PORT}/api/${API_VERSION}`;
const APP_VERSION = '1.0.0';

export default {
    API_HOST,
    API_PORT,
    API_VERSION,
    API_URL: `http://${API_HOST}:${API_PORT}/api/${API_VERSION}`,
    APP_VERSION,
}